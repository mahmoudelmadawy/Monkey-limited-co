# Copy this file to .env and fill in your Postgres credentials
PGHOST=localhost
PGPORT=5432
PGDATABASE=monkey_limited_co
PGUSER=postgres
PGPASSWORD=changeme

# Alternatively, set a single connection string instead of the PG* vars above:
# DATABASE_URL=postgres://user:password@localhost:5432/monkey_limited_co

PORT=3000
